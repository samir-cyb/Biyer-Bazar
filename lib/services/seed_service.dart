/// SeedService — no-op now that Supabase is the primary data store.
/// Dev seed data is managed directly in Supabase via the admin panel or SQL.
class SeedService {
  static void seed() {
    // Seeding is handled in Supabase.
    // This stub exists to prevent breaking callers.
  }
}
